db-build:
script to build the database, built automatically using the schema class in the main test

mysql-connector:
make sure this library is referenced in the project build path and that the path is correct

app.properties:
app configuration file - please edit username, password, and the url if needed